spark:
  app_name: BankingSparkETL

datasets:
  branches:
    path: data/input/branches.parquet
    format: parquet

  employees:
    path: data/input/employees.parquet
    format: parquet

  customers:
    path: data/input/customers.parquet
    format: parquet

  accounts:
    path: data/input/accounts.parquet
    format: parquet

  cards:
    path: data/input/cards.parquet
    format: parquet

  loans:
    path: data/input/loans.parquet
    format: parquet

  transactions:
    path: data/input/transactions.parquet
    format: parquet

outputs:
  customer_transaction_summary:
    path: data/output/customer_transaction_summary
    format: parquet

  ranked_customer_transactions:
    path: data/output/ranked_customer_transactions
    format: parquet